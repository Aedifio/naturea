# Copy to .env and fill in (never commit .env)
# Supabase — Dashboard → Settings → API
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Sentry — Project Settings → Client Keys (DSN)
SENTRY_DSN=
# Optional — defaults to naturea-portal@<package.json version>
# SENTRY_RELEASE=
